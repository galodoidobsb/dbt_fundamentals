sources:
  - name: jaffle_shop
    database: raw
    schema: jaffle_shop
    config:
      freshness:
        warn_after:
          count: 72
          period: hour
        error_after:
          count: 30
          period: day
    tables:
      - name: customers
        # config:
        #   loaded_at_field: _etl_loaded_at
      - name: orders
        # config:
        #   loaded_at_field: _etl_loaded_at
